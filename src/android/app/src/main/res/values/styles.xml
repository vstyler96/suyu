<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Custom button styles -->
    <style name="InGameMenuOption" parent="Widget.Material3.Button.TextButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif-condensed</item>
        <item name="android:gravity">center_vertical|left</item>
        <item name="android:paddingLeft">32dp</item>
        <item name="android:paddingRight">32dp</item>
    </style>

    <style name="suyuSlider" parent="Widget.Material3.Slider">
        <item name="tickVisible">false</item>
        <item name="labelBehavior">gone</item>
    </style>

    <style name="suyuMaterialDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="colorPrimary">@color/suyu_primaryContainer</item>
        <item name="colorSurface">@color/suyu_primaryContainer</item>
        <item name="colorSecondary">@color/suyu_primary</item>
        <item name="android:textColorLink">@color/suyu_primary</item>
        <item name="buttonBarPositiveButtonStyle">@style/suyuButton</item>
        <item name="buttonBarNegativeButtonStyle">@style/suyuButton</item>
        <item name="buttonBarNeutralButtonStyle">@style/suyuButton</item>
    </style>

    <style name="suyuButton" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textColor">@color/suyu_primary</item>
        <item name="rippleColor">@color/suyu_inversePrimary</item>
    </style>

</resources>
